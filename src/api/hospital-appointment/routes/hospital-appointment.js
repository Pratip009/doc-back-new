'use strict';

/**
 * hospital-appointment router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::hospital-appointment.hospital-appointment');
